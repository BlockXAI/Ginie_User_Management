# =============================================================================
# EVI Admin Console - Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
#
# For LOCAL DEVELOPMENT with a REMOTE backend (Railway):
# Leave NEXT_PUBLIC_API_BASE_URL empty. Requests go through Next.js rewrite
# proxy (configured in next.config.js), making them same-origin.
#
# NEXT_PUBLIC_API_BASE_URL=
#
# For LOCAL DEVELOPMENT with a LOCAL backend:
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
#
# For PRODUCTION (Vercel) with a remote backend:
# NEXT_PUBLIC_API_BASE_URL=https://usermanagementapis-production.up.railway.app

# -----------------------------------------------------------------------------
# Backend URL for Next.js Rewrites (server-side only)
# -----------------------------------------------------------------------------
# Used by next.config.js for API proxy rewrites during development.
# Not needed if NEXT_PUBLIC_API_BASE_URL is set.
#
# BACKEND_URL=https://usermanagementapis-production.up.railway.app
